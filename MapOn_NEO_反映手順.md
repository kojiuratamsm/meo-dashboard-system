# MapOn NEO 反映手順

## やること(2つ)

### 1. データベースの設定(Supabase・1回だけ)
1. Supabase の管理画面を開く
2. 左メニューの「SQL Editor」→ 右上の「+」(New query)
3. このフォルダの `supabase/MapOn_setup.sql` をテキストエディタで開き、中身を**すべて**コピーして貼り付ける
4. 右下の「Run」を押す → 「Success. No rows returned」と出れば完了

※何度実行しても大丈夫です。

### 2. 本番に反映
いつもの方法で、このフォルダを本番(Vercel)に反映してください。

**必ず 1 → 2 の順番で行ってください。** 2 を先に行うと、マスター画面に「データベースの初期設定が完了していません」と表示されます(その場合は 1 を行えば直ります)。

---

## 使い方
- マスター画面の左メニュー「MapOn NEO」から、アンケートを作成・公開し、QRコードを発行します。
- 店舗オーナーに管理画面のログインを渡す場合は、左下の「申し込みフォームURL取得」でURLをコピーして、手動で送ってください。
- 店舗オーナーは、クライアント画面の「MapOn NEO」で、回答の確認とQRコードのダウンロードができます(編集はできません)。

---

## 必要になったときだけ

### パスワード再設定リンク(オーナーがパスワードを忘れたとき)
マスター画面のクライアント一覧に「再設定リンク」ボタンがあります。押すとリンクが表示されるので、コピーしてLINEなどでオーナーに送ります。
このボタンを使うには、最初に次の2つの設定が必要です。

**A. Vercel に鍵を登録する**
1. vercel.com を開いてログイン → MapOn のプロジェクトを開く
2. 上のメニュー「Settings」→ 左の「Environment Variables」
3. 「Key」に `SUPABASE_SECRET_KEY` と入力
4. 「Value」には、Supabase の管理画面 → 左下の歯車「Project Settings」→「API Keys」にある **Secret key**(`sb_secret_` で始まる文字列)をコピーして貼り付ける
5. 「Save」を押す → 上のメニュー「Deployments」→ 一番上の行の「…」→「Redeploy」

※この鍵は、チャットやメールには貼らないでください。

**B. Supabase に戻り先のURLを登録する**
1. Supabase の管理画面 → 左メニュー「Authentication」→「URL Configuration」
2. 「Site URL」を `https://www.mapon-meo.com` にする
3. 「Redirect URLs」の「Add URL」で `https://www.mapon-meo.com/reset-password` を追加 →「Save」

### 平文パスワードの削除(後日)
クライアント一覧には、もうパスワードは表示されません。ただ、過去に登録したパスワードがデータベースに平文で残っています。
オーナーの皆さんに「パスワードの再設定」をお願いし終わったら、`supabase/あとで実行_平文パスワード列の削除.sql` を 1 と同じ方法で実行してください。
