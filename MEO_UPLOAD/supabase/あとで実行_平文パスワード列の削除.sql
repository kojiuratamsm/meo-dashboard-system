-- ============================================================
-- MapOn 第0段階 ③平文パスワード列の削除
--
-- ※実行の条件:
--   ・MapOn_setup.sql の実行と、今回の画面の変更が本番に反映済みであること
--     (新しい画面はこの列を使いません)
--   ・既存クライアントへの「パスワード再設定のお願い」の連絡が済んでいること(未決U9)
--
-- 補足:実際のログインは Supabase Auth が保持するハッシュ化されたパスワードで行われています。
--       この列は「画面に表示するための平文の控え」なので、削除してもログインには影響しません。
--       ただし一度削除すると、平文の控えは元に戻せません(それがこの作業の目的です)。
-- ============================================================
alter table public.clients drop column if exists temp_password;
